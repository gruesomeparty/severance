# Severance handover + local runtime state — never committed
.severance/

# Local Claude Code settings (per-machine; e.g. Severance test caps)
.claude/settings.local.json

# macOS
.DS_Store

# Swift / SwiftPM (menu bar app, M5)
.build/
apps/menubar/.build/
apps/menubar/.swiftpm/
apps/menubar/*.app
*.xcodeproj
Severance-macos.zip

# Python (local schema validation)
.venv/
venv/
__pycache__/

# Node (tooling only)
node_modules/
